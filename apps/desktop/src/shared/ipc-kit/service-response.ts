// Moved to the shared contracts package; this shim keeps the app's old import
// paths working. New code should import from "@mains/contracts/service-response".
export * from "@mains/contracts/service-response";
